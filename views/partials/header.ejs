<!DOCTYPE html>
<html>

<head>
    <% if (typeof title !=='undefined' ) { %>
        <title>
            <%= title %>
        </title>
        <% } %>
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
            <!-- <link rel="stylesheet" href="/stylesheets/style.css" /> -->
</head>
<header class="navbar navbar-expand-lg navbar-dark bg-dark p-3">
    <div class="container">
        <% if (typeof username !== 'undefined') { %>
            <a href="/books" class="navbar-brand mb-0">Catálogo de Libros</a>
        <% } else{ %>                        
            <p class="navbar-brand mb-0">Libreria Online</p>
        <% } %>

        <div class="ms-auto">
            <% if (typeof username !== 'undefined') { %>
                <span class="navbar-text text-light me-3">Bienvenido, <%= username %>!</span>
                <a href="/books/add" class="btn btn-primary me-2">Agregar Libro</a>
                <a href="/users/logout" class="btn btn-outline-light">Cerrar sesión</a>
            <% } else{ %>
                    <a href="/users/login" class="btn btn-primary">Iniciar sesión</a>
                    <a href="/users/register" class="btn btn-primary">Registrar</a>
            <% } %>

        </div>
    </div>
</header>

<body>

    <!-- Contenido principal -->
    <main class="container my-5">